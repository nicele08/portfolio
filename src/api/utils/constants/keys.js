const { REACT_APP_DEFAULT_API_DOMAIN } = process.env;
const { REACT_APP_DEFAULT_JWT_SECRET } = process.env;
const { TOGGLE_SIDEBAR } = process.env;

const Keys = {
  REACT_APP_DEFAULT_API_DOMAIN,
  REACT_APP_DEFAULT_JWT_SECRET,
  TOGGLE_SIDEBAR,
};

export default Keys;
